class Delegator:
    pubkey : str    # For toy-project purposes, we store delegator IDs as string, i.e. Validator_no1, Validator_no2, instead of delegator pubkeys
    balance : float
    quota : float